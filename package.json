{
  "name": "cluster",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,agent,frontend -c cyan,magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix agent\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && cd agent && pip install -r requirements.txt"
  },
  "prisma": {
    "seed": "ts-node --project backend/tsconfig.json prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "concurrently": "^8.2.2",
    "dotenv": "^17.3.1",
    "prisma": "^7.5.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0"
  }
}
