{
  "name": "cluster",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,agent,frontend -c cyan,magenta,green \"npm run dev --prefix backend\" \"npm run dev --prefix agent\" \"npm run dev --prefix frontend\"",
    "start": "concurrently -n backend,agent,frontend -c cyan,magenta,green \"npm run dev --prefix backend\" \"python3 agent/main.py\" \"npm run start --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix electron && cd agent && pip install -r requirements.txt",
    "electron:dev": "npm run dev & sleep 8 && npm run dev --prefix electron",
    "electron:build": "npm run build --prefix electron",
    "electron:dist": "npm run build --prefix frontend && npm run dist --prefix electron",
    "electron:dist:win": "npm run build --prefix frontend && npm run dist:win --prefix electron",
    "electron:dist:mac": "npm run build --prefix frontend && npm run dist:mac --prefix electron"
  },
  "prisma": {
    "seed": "ts-node --project backend/tsconfig.json prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1",
    "prisma": "^6.7.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "concurrently": "^8.2.2"
  }
}
